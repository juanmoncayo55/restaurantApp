import { createContext } from 'react'

const FirebaseContext = createContext();

export default FirebaseContext;

/*
	1. Primero importamos el createContext de react, luego creamos una instancia de createContext, por ultimo lo exportamos
*/